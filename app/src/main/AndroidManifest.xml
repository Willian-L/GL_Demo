<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gd_lg">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GD_LG">
        <activity android:name=".activities.WeiXinActivity"></activity>
        <activity android:name=".activities.LinearLayoutActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.FirstActivity" />
        <activity
            android:name=".activities.GetExtraActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activities.LinWeiLiActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>